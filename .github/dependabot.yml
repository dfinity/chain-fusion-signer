# Please see the documentation for all configuration options:
# https://docs.github.com/en/enterprise-cloud@latest/code-security/dependabot/dependabot-version-updates

version: 2
updates:
  - package-ecosystem: "cargo"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      # These crates share the `signature` trait crate. Bumping one of them alone
      # leaves the workspace with two `signature` major versions and the Schnorr
      # integration tests no longer compile, so they must move together.
      rustcrypto:
        patterns:
          - "signature"
          - "ed25519-dalek"
          - "k256"
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      ic-identity:
        patterns:
          - "@dfinity/identity"
          - "@dfinity/auth-client"
